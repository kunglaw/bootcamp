import { VM } from './VM';
export interface Connection {
    el: HTMLIFrameElement;
    id: string;
    pending: Promise<VM>;
    vm: VM;
}
export interface RequestData {
    type: string;
    payload: {
        __reqid?: string;
        [key: string]: any;
    };
}
